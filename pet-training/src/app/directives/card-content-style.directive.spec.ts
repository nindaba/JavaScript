import { CardContentStyleDirective } from './card-content-style.directive';

describe('CardContentStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardContentStyleDirective();
    expect(directive).toBeTruthy();
  });
});
